<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "reelio" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans+Condensed:300,700|Oswald:400,700,300' rel='stylesheet' type='text/css'>
  </head>

  <body>

  <%= render 'layouts/header' %>

    <div id="wrapper">
      <% if flash[:alert] %>
        <%= flash[:alert] %>
      <% end %>
      <% if flash[:notice] %>
        <%= flash[:notice] %>
      <% end %>
      <%= yield %>
      <div class="clear"></div>
    </div>

    <% if current_user && params[:id] %>
      <%= render 'layouts/message' %>
    <% end %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
